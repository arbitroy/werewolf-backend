-- V1: Initial Schema (Baseline - represents current state before refactoring)
-- This should match what Hibernate already created

-- This migration is just for documentation
-- Flyway will baseline at this version when run on existing database
SELECT 1;